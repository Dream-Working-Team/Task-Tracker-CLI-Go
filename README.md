# Task-Tracker-CLI-Go
A minimal, dependency-free Command Line Interface (CLI) task tracker built in Go. It manages daily tasks utilizing a local JSON file for persistent storage, relying exclusively on Go's standard library for file system operations and argument parsing.
